Aria.tplScriptDefinition({
  $classpath: 'modules.foo.bar.TestScript',
  $extends: "modules.foo.bar.ParentScript",
  $dependencies: ['modules.foo.bar.UnusedDep', 'modules.foo.bar.ImportantDep'],
  $constructor: function() {},
  $prototype: {

    $dataReady: function() {
        // comment
        this.foo = "foo";
        this.bar = "bar";
    },

    something: function(param) {
        modules.foo.bar.ImportantDep.something(param, "anotherParam");
    }
  }
});